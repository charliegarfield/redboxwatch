# Core
httpx>=0.27
pyyaml>=6.0
python-dateutil>=2.9

# Crawling / rendering (phase 2)
playwright>=1.44
beautifulsoup4>=4.12
lxml>=5.2
pdfminer.six>=20231228
pypdfium2>=4.30       # rasterize archived PDFs into evidence exhibits (archiver)
pillow>=10.0          # transcode evidence screenshots PNG -> WebP (archiver)

# Classifier (phase 3)
anthropic>=0.40

# Dev
pytest>=8.0
