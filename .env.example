# openFEC API key (api.data.gov). DATA_GOV_API_KEY / OPENFEC_API_KEY are
# accepted as aliases.
FEC_API_KEY=
# Anthropic Messages API key (billable)
ANTHROPIC_API_KEY=
FIREWORKS_API_KEY=
OPENAI_API_KEY=
WAYBACK_S3_KEY=
WAYBACK_S3_SECRET=
SERPER_KEY=

# NOTE: the bsky-poster worker's secrets (RUN_TOKEN, BSKY_APP_PASSWORD) are
# wrangler secrets (`wrangler secret put` in workers/bsky-poster), not .env
# variables — nothing in the Python pipeline reads them.
